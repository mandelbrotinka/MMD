# Additional information necessary to succesfully run the code

## Important! -> Datasets

Add in the ***datasets*** folder the extracted files of the dataset. The downloaded archive from the dataset website has another archive in it. 

Please don't extract "covtype.data.gz" again.

In the end, the dataset path should look like this: "datasets\covtype.data.gz".

## Check the results

After running the whole notebook, the outputs of the runs are also available in **clustering_results.txt**.